import os

дом = os.getenv("HOME")

настройки = {
    "имяФайла": "расписание",
    "путьКдире": "%s/.config/расписание" % дом,
    "дираАрхива": "%s/.config/архив_расписания" % дом,
    "ширина": 1000,
    "высота": 700,
}

